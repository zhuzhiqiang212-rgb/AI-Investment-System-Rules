# 109 · Code 回报 · 轮80（AT·并入历史+补两闸+层命名）· 2026-08-02

岗位：Claude Code。★本轮不出产品。全部已做并验证。

## AT1 · 并入七条历史判断 ✅
- **AT1-1**：从 `judgment_history_opus5_20260731_0802.json` 并入 **7 条**（⑥持仓层×4 + ⑦复盘层×3·全『已证伪』）进 `judgment_scorecard.json`。★**每条带「事后补登·一次性历史回填」标记**（违反登记须同时的规矩·留痕不洗白）；`★错在哪` 归一到 `错在哪`（数据错/逻辑错/口径错）。
- **AT1-2 certainty 重算**：⑥持仓层 +4 已证伪、⑦复盘层 +3 已证伪 → **⑥ 已验证4·命中0·命中率0.0%**；**⑦ 已验证3·命中0·命中率0.0%**（★这七条是第一批有确定结论的样本·命中率如实反映 **0/7**）。
- **AT1-3 派生分布（进产品）**：**四类错因分布 = 数据错3 / 逻辑错2 / 口径错2 / 时机错0**；**按层分布（出错判断）= ⑥持仓层4 / ⑦复盘层3**。两张表已进 `scorecard_summary_{date}.json`。
- **★副产（AS6 现有真样本）**：本体系**最弱层**从「全部起步期」变为**具体指向 ⑥持仓层（命中率 0%）**——产品现在真能看出哪层最弱。

## AT2 · 补两道尚未装闸的 ✅
- **AT2-1 C级来源不得输出精确数**（加进 `judgment_scorecard_gate`）：依据等级=C 的判断文本含小数点后≥2位精确值 → **FAIL**。**实测**：合成「Arm 占 75.34%」→ rc=6 FAIL「C级不得输出小数点后≥2位·应改『大部分/约』」。真数据 PASS（C级种子 E上行=None·不出精确收益·符合『B/C不出预期收益』）。
- **AT2-2 未读源码不得对机器行为下判断**（新建 `unread_source_gate.py`·★**这条拦的是 Opus5·不是 Code**·接管线 ⑪d 告警）：任务包/正文出现「闸/脚本名 + 判定词（判对了/逻辑不动/判错…）」而**同文件无对应源码路径引用（scripts/xx.py）或『读过…py』声明** → 告警。做法即 AT2-2 要求的「须先声明读过该脚本」。

## AT3 · 层命名统一 ✅
`certainty_ledger` / 记分卡 / 闸的层名统一为蓝图口径：**④板块地图→④板块轮动**、**⑤个股研究→⑤机会池**（与蓝图+八步流程尺一致）。七层现＝①世界观/②国家战略/③资金流动/**④板块轮动**/**⑤机会池**/⑥持仓层/⑦复盘层。

## 闸实测汇总
- judgment_scorecard_gate：**PASS**（每条有条目·无未命中/已证伪缺错因·确定性派生一致）+ **7 条 AS1-3 告警**（事后补登·登记日≠判断产出日·留痕）。
- AT2-1 C级精确数：合成 rc=6 FAIL ✔。
- AT2-2 未读源码：告警机制建好（拦 Opus5）。

## ★一句话
七条历史判断已并入（带事后补登标·不洗白）、命中率如实 0/7、四类错因(数据错3/逻辑错2/口径错2)+按层分布(⑥4/⑦3)两表进产品、**最弱层现具体指向⑥持仓层(0%命中)**；补了两道闸——**C级不得输出精确数**(合成证 rc6)、**未读源码不得判机器行为**(拦Opus5·告警)；层命名统一到蓝图口径(④板块轮动/⑤机会池)。本轮不出产品。
