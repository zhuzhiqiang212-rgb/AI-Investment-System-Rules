# 93 · Code 回报 · 轮67（AF1–AF3·渲染器合并）· 2026-07-31

岗位：Claude Code（技术施工）。本轮唯一任务=把轮65 手工渲染器 `render_0730_final.py` 的八项整改**并入唯一渲染器 `render_3layer.py`**，合并后删/标废手工渲染器，空跑验证（仍不出品）。以下据实报，含**一处如实标的既有阻断（与合并无关）**，不假报。

---

## AF1-1/AF1-3 · 八项搬迁对照表（render_3layer 合并前 → 后）

在 render_3layer 新增两函数 `_z4_two_segment_block(date)`（读 `data/risk/z4_two_segment_{date}.json`）+ `_z4_forecast_note(date)`（读工作版 forecast 的「口径标注」字段），并在第一屏注入点（L1 层顶·原 `_target_gap_block()` 处）改为 **`_z4_two_segment_block(date) or _target_gap_block()`**（z4 数据在→用两段；缺→回落旧块·安全增量）。

| # | 项 | 合并前 render_3layer | 合并后 render_3layer（实测在产品输出中） |
|---|---|---|---|
| 1 | Z4 两段报法 | 无·旧 `_target_gap_block` 给**单一「距+40% 还差X」+ 16.87%** | ✔ 已算清/未算清**分列**·★不给单一混合缺口数 |
| 2 | 点值口径 E[上行] | 无（旧块混合概率出 16.87%） | ✔ Σ概率×point_value（z4 json 由 `compute_expected_upside` 算）·标「点值口径 E[上行]=Σ概率×点值」 |
| 3 | 参数出处四等级 | 无 | ✔ 已算清=特+A级；未算清=B+C级（标「无可用估值锚」·**不出预期收益**） |
| 4 | 净现金扣减（任天堂¥1,940） | 已有（个股卡·旧） | ✔ 保留未动（¥1,940 净现金标注仍在个股卡） |
| 5 | 单只超限告警 | 无 | ✔ 单只>20% 显性告警「超单只20%上限 X个百分点」·标出处·**不给处置建议** |
| 6 | 退出类型（第一三共） | 仅异常股退出估值 | ✔ Z4 卡显式「退出（第一三共·财务质量）X%·**不进情景计算**」 |
| 7 | 微软口径标注 | 无 | ✔ 红框「财报前 EPS $19.09…该负值偏悲观」（数据驱动·读 forecast「口径标注」字段） |
| 8 | 机会层不填价位 | 无 | ✔ 「候选估值未算·Opus5 周末补·**本产品不填任何候选或买卖价位**」 |

**实测（AF2 独立渲染·完整数据 20260730）**：`render_3layer --dry-run` 输出 **810,818 字节·乱码 EFBFBD=0·八项自查 8/8 全 ✔**。

★配套：手工渲染器读的 `z4_two_segment_{date}.json` **原不在管线里生产**（轮65 手工建的）。为让「按管线跑即含八项」，新建 `scripts/z4_two_segment_build.py`（取价/市值/等级取自 `target_gap_{date}` 单一真相源·点值口径）并接进管线目标层 **⑥k**（轮66 已接的 target_gap 之后）。

## AF1-2 · 手工渲染器已标废弃
- `scratchpad/render_0730_final.py` 顶部加 `# DEPRECATED 轮67` 头；副本归档至 `00_请先看这里/_历史归档/废弃渲染器/render_0730_final_DEPRECATED_轮67.py`（带醒目废弃说明）。
- **唯一渲染器 = `scripts/render_3layer.py`**；正式产品一律经 `daily_auto_produce.py → render_3layer.py` 出。

---

## AF2 · 合并后空跑（仍不出品）

### AF2-1 两种验证
1. **管线 dry-run**（`daily_auto_produce.py --date 20260730 --dry-run`）：**40 步·闸调用 10·今日接入模块被调用 10/10**（含新 ⑥k z4两段缺口 rc=0、新 ⑬d render_3layer(DRY) 被管线调用）。→ **证明渲染器已被管线调用、z4 生产已在管线里。**
2. **渲染器独立 dry-run**（完整数据 20260730·渲到临时路径）：**八项自查 8/8 全 ✔·810,818 字节·乱码 0**（见上表）。→ **证明渲染器输出实含八项。**

★管线 dry-run 中 ⑬d 渲染 8 项自查 rc=6（未过）——因当时无实时 OpenD，dry-run 重建 target_gap 时数据步失败→z4 段落空→渲染无数据。这是 **OpenD 不可用的连带**，非渲染逻辑问题；渲染器含八项的**决定性证据是完整数据下的独立渲染（8/8）**。

### AF2-2 无副作用（已核）
- **不碰台账**：`auto_produce_runs.json` 末条仍是 07-31 07:30 FAIL（dry-run 未追加）。
- **不碰主控**：`★开工必读_主控文件.html` 未被 dry-run 改。
- **跑完清理**：备份 61 个 2026073* 数据→dry-run→**全部恢复**、删 2 个 dry-run 新建物（dry_run_20260730.json / z4_two_segment_20260730.json）；临时渲染物已删。G盘无残留。轮65 产品与 z4_two_segment_20260731 完整保留。

---

## AF3 · ★一句话回答（现在按 daily_auto_produce.py 正式跑，产出的产品是否已含今天全部整改）

**内容上——是**：唯一渲染器 render_3layer 的输出实测已含全部八项整改（8/8·独立渲染实证）。

**但要真正出品——还差清一个【与本次合并无关的既有阻断】**：render_3layer 现有一条 **L12 出厂闸 FAIL**——「防御」集中度**双源**：`systems_soul_build.py` 算 **13.2%（＜15% 下限→拍板卡说「该加防御」）** vs render_3layer 的 conc 现算 **15.5%（＞下限→「不用加」）**，两者**结论相反**。此闸**去掉我这次的 Z4 块也照样 FAIL**（已验证）→ 确认是既有缺陷、非本轮合并引入。**它属"哪个防御口径权威"的口径裁定（Opus5 域），我不擅自选一个数。** 另有护城河超期（>16天）阻断，已由管线 ⑬a 自动重评前置解决（非阻断）。

→ **结论**：八项已并入唯一渲染器（合并任务完成）；首次正式生产前，请 Opus5 裁定防御集中度口径（13.2 还是 15.5），清掉 L12 这条既有双源闸，即可出品。

---

## 改动文件
- `scripts/render_3layer.py`：+`_z4_two_segment_block`/`_z4_forecast_note`·第一屏注入 Z4 两段·+`--dry-run/--out`（AF2 渲不出品）。乱码 0。
- `scripts/z4_two_segment_build.py`（新）：Z4 两段缺口生产（点值口径·取自 target_gap 单一真相源）。乱码 0。
- `scripts/daily_auto_produce.py`：接 ⑥k z4两段缺口·dry-run 增 ⑬d 渲染自查。乱码 4（**全为原第265行注释里故意展示的 GBK 乱码示例**·我引入 0）。
- `data/forecast/forecast_2026-07-30.json`：微软加「口径标注」字段（item7 数据驱动）。乱码 0。
- `scratchpad/render_0730_final.py` → 标废弃+归档 `_历史归档/废弃渲染器/`。
- **本轮未出产品**；台账/主控未动；dry-run 数据痕迹已全清。
