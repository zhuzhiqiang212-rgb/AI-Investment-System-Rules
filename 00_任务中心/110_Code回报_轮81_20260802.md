# 110 · Code 回报 · 轮81（AU·修记分卡样本偏差）· 2026-08-02

岗位：Claude Code。★本轮不出产品。样本偏差已修·验证分三类不混算。

## AU1 · 「已验证」分三类·不混算 ✅
- **AU1-1**：judgment_scorecard 每条加 `★验证来源`＝**自查证伪 / 到期验证 / 事件证伪**。回填现有条目（JH-*历史=**自查证伪**·forecast种子=**到期验证**）。
- **AU1-2**：certainty_ledger **分三类统计·禁合并成单一命中率**。每层出：到期验证{数/命中/命中率}、自查证伪{数}、事件证伪{数}、待验数。
- **AU1-3 升档只看「到期验证」类**：连续≥10条到期验证且命中率≥60%→中；≥20且≥70%→高。**★自查证伪不计入升档分母**（否则越自查越降级=反向激励）。闸的 `_derive_cert` 同步改为只算到期验证。
- **AU1-4 自查发现率（正面指标）**：单列 `自查证伪7 / 到期验证20`·说明「越高=自查越有效·是正面指标不是负面·度量自查有效性非判断能力·不与命中率混算」。

## AU2 · 产品三数并列（防误读）✅
`scorecard_summary` 的 `★AU2产品三数并列`（三个必须并列·★不许只显单一命中率）：
| 数 | 实测值 |
|---|---|
| ★到期验证命中率 | **待样本（首条到期日 2027-07-30·据 forecast verdict_date）** |
| ★自查发现的错 | **7 条（数据错3 / 逻辑错2 / 口径错2）** |
| ★待验判断 | **20 条** |
★呈现铁律已写死进数据：「这三个数必须并列·**不许只显单一『命中率0%』**（自查证伪≠判断能力·那是误导）」。

## 修复的核心 bug
上轮 certainty 把 7 条自查证伪算进「已验证」→ 命中率 0.0%（**度量的是自查有效性·不是判断能力**）。本轮拆开：⑥持仓层＝到期验证0(命中率**待样本**)+自查证伪4；⑦复盘层＝到期验证0+自查证伪3。**最弱层判据改为只用到期验证**（当前样本=0→「首条到期后才有」），不再被自查证伪的 0% 误导。

## ★一句话
记分卡样本偏差已修——「已验证」拆成三类（自查证伪/到期验证/事件证伪）不混算：**升档只看到期验证**（自查证伪不进分母·防越自查越降级的反向激励），**自查发现率单列为正面指标**（当前 7 条·数据错3/逻辑错2/口径错2）；产品**三数并列**（到期验证命中率＝待样本·自查发现的错7条·待验20条），**不再出现误导性的单一「命中率0%」**。（注：现有 1y forecast 种子首条到期日=2027-07-30；若有 08-03 短周期预测待另行种入。）本轮不出产品。
