# TASK-2026-07-02-016｜渲染14层经线完整日报

## 授权
- 董事长已授权执行。
- 本任务只生成正式 HTML 日报，不下单、不发布、不改账户数据。

## 范围
- 新建 `00_请先看这里/日报_2026-07-02_完整经线版.html`。
- 按董事长提供的 14 层经线原文排版，每层一个 `<h2>` 章节。
- 顶部保留风险声明：非实时、非下单指令、最终决策在董事长、Claude 非持牌投顾。
- 数据源引用 `latest_market_snapshot.json` 与 `unified_holdings_latest.json`。
- 占位与待补层显式标注，不填假数。

## 铁律
- UTF-8 写入，写完重读确认无乱码。
- 不改判断内容，不自行增删数字。
- 不碰 V0，不改账户、持仓、现金数据。
- 生成后运行 `scripts/quality_chain_check.py` 四道闸门并记录结果。
