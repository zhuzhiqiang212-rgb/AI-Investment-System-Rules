# TASK-2026-07-02-023｜机会发现改链驱动 T3

## 授权
- 董事长已定稿授权。
- 本任务建设链驱动机会发现脚本，只读行情，不下单，不发布。

## 范围
- 新建 `scripts/opportunity_chain_driven.py`。
- 读取 `data/evidence_chain/daily_YYYYMMDD.json`，无正式求证表则退出并提示需先填当日求证表。
- 支持 `--demo` 使用示例求证表跑通流程，输出 `data/opportunities/chain_opportunities_YYYYMMDD_DEMO.json`，明确标注 DEMO/流程验证/非正式机会池。

## 总则第六条落实
- 个股名单不写死，每日由当日求证表与 OpenD 板块成分现算。
- 随证据链总闸与战略指向变化自动激活或收缩节点。
- 节点映射只锚定义，不锚答案。
- 输出头部带当日指纹，可回溯总闸、激活节点、口径与推导理由。

## 铁律
- UTF-8 写入，写完重读无乱码。
- 只读只筛，不判估值，不下单，不发布。
- 拉不到数据标待补，不臆造。
- 不动 V0、账户、持仓、宪法、总则。
