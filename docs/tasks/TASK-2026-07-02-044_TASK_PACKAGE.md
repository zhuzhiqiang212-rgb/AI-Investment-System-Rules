# TASK-2026-07-02-044 任务包

【总则对照】对照六条：第一条→催化维从当日机会池与持仓动态进入，不从持仓反推；第二至五条→不改证据链骨架、求证表、驱动、复盘；第六条防静态化→自评：财报日历随日期动态刷新，横比随线上标的与模型实例目录变化，事件驱动诚实标待接，不写静态死值。

## 任务
落地催化维数据源 v1：财报日历结构与模型实例横比真做，事件驱动保留动态占位。

## 范围
- 新建 scripts/catalyst_dimension_build.py。
- 读取当日机会池候选与统一持仓，不锁个股名单。
- 读取 data/valuation/model_instances 下已有实例，生成同模型横比位。
- 输出 data/catalyst/catalyst_YYYYMMDD.json。

## 铁律
- UTF-8 写入，写完重读确认无乱码。
- 只读本地数据，不下单，不发布。
- 缺财报日期不伪造，标待人工或后续接口填入。
- 实时新闻与监管事件源未接入前，事件驱动只留动态占位。
