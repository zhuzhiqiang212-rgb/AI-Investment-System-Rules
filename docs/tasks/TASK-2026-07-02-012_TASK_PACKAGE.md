# TASK-2026-07-02-012 任务包：建质量链＋SOP V1

## 授权
- 董事长已签字授权。
- 本任务只建设“数据→估值→风险→动作→日报”质量链 V1，不改账户、不下单、不自动发布。

## 工作范围
1. 新建 `docs/QUALITY_CHAIN_SOP_V1.md`。
2. 新建 `scripts/quality_chain_check.py`，只读判定四道闸门。
3. 在 `docs/02_REPORT_STANDARD_V7_完整版.md` 加质量链脚本指针，不改已有 7 条红线正文。
4. 用今天四账户日报和统一持仓库干跑一次。

## 铁律
- UTF-8 写入，写完重读。
- 脚本只读只判，不改日报、不改数据、不下单、不发布。
- 缺数据/占位按规则标记，不伪造达标。
- 未在现有文件中找到 Claude 已落档细项的地方，标“待 Claude 确认”。

## 回报
- 新建文件清单。
- 脚本试跑结果。
- 每处改动前→后。
