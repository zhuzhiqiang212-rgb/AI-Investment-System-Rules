# TASK-2026-07-02-026｜持仓改受检对象 T5

## 授权
- 董事长已授权。
- 本任务落地总则第一条：持仓是证据链推导的结果与检验对象，不是起点。

## 范围
- 新建 `scripts/holdings_review_against_chain.py`。
- 读取当日求证表与统一持仓库，只读生成持仓受检报告。
- 输出 `data/holdings/holdings_review_YYYYMMDD.json`。
- 支持 `--demo` 输出 `holdings_review_YYYYMMDD_DEMO.json`，标注 DEMO 非正式。

## 总则第六条落实
- 每日现算：每次读取当日求证表和最新持仓库。
- 随链变：总闸与战略指向变化会改变激活节点。
- 不写死持仓归类：是否符合由当日板块成分动态判定，非战略标签来自统一持仓库现有字段。
- 带指纹：输出总闸、激活节点、求证表来源、generated_at、mode。

## 铁律
- 只读，不下单，不发布。
- 不建议具体买卖，只出符合、不符合、待补对照。
- 拉不到数据标待补，不臆造。
- UTF-8 写入，写完重读无乱码。
- 不动 V0、账户、持仓、宪法、总则。
