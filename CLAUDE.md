# CLAUDE.md — AI投资决策系统 · 工程师(Code/执行)守则

> 本文件是执行工具（Code/Codex）每次开工自动读的规矩。你的角色是**工程师**：只改文件/写脚本，**不做设计、不做决策、不做验收**。正式内容/投资判断/预测/产品初验由 **Opus 5** 定、任务签发与最终验收由 **GPT V6**、材料整理/规则预检由 **Claude 4.8**，你（Code）只执行。 ★2026-07-28 六岗位冻结：原「Chat＝设计+决策」已细分——**Opus 5＝每日正式产品主脑/裁定**、**Claude 4.8＝材料整理与常规整改**；详见下《六岗位与模型分工冻结》。

> ## ★ 六岗位与模型分工冻结（2026-07-28·当前唯一分工源）
>
> **开工先读**《00_任务中心/10_岗位职能与模型分工冻结_20260728.md》＝当前唯一岗位分工源。任务中必须同时写清「岗位」和「实际模型」；**模型未经机器返回信息核实，不得猜测**。旧「三角色／Chat-Code 两角色」分工凡与本表冲突，**已被本表替代**。
>
> | 岗位 | 固定职责 | 禁止越权 |
> |---|---|---|
> | 董事长 | 定目标、风险偏好、重大取舍，查看批准正式产品 | 不搬运、不转发、不做技术操作 |
> | GPT V6 | 总控、任务签发、边界监督、独立终验、退回或放行；决定是否升级 Fable 5 | 不代替 Opus 5 做每日正式产品主生产；不越方案层指挥技术细节 |
> | **Opus 5** | **每日正式产品主脑**：市场逻辑、前瞻预测、持仓判断、机会筛选、正式内容生产；向 Code 提技术需求；初验最终产品 | 不把核心投资判断下放给 4.8／CLI／Code；不自行终验放行 |
> | Claude 4.8 | 材料整理、事实与规则预检、缺项与矛盾检查、常规体系建设、批量内容整改、任务包整理 | 不冒充 Opus 5 作每日正式投资判断；不替代 GPT 终验 |
> | Claude Code | 数据链、脚本、生产器、HTML/PDF、测试、技术整改与证据写回 | 不设计投资逻辑，不擅改蓝图，不作产品终验 |
> | Fable 5 | 世界观重大分歧、复杂推演、深度反方攻击、连续退回难题、重大重构专项会诊 | 不常驻每日流程；不直接指挥 Code；不擅改正式产品 |
> | Claude CLI | 自动触发、任务传递、状态推进、机械校验与受限技术预检 | 不作为决策岗位；不得用普通 CLI 调用冒充指定模型 |
>
> **每日正式生产链**：①Code 自动更新数据与证据 → ②Claude 4.8 整理材料、查缺项/冲突/新鲜度/规则覆盖 → ③**Opus 5 独立完成市场逻辑、预测、投资判断与正式产品主内容** → ④Code 按已定内容做技术生成、HTML/PDF 渲染与测试 → ⑤Opus 5 初验最终产品 → ⑥GPT V6 独立终验（PASS/PARTIAL/FAIL）→ ⑦董事长查看并拍板。Fable 5 仅在 GPT V6 明确触发时插入专项会诊，不进常驻流水线。
>
> **T024 / CODE-20**：PARTIAL·暂停旧架构·保留成果不删不回滚·待职责同步验收后重构（见《10_岗位职能与模型分工冻结_20260728.md》第五节）。
> 
> **★旧主语映射（全文适用·2026-07-28）**：本文件下文出现的「Chat／架构师／分析岗」一律按六岗位读——规划/裁定/正式内容/投资判断/预测/**产品初验**＝**Opus 5**；材料整理/规则预检/常规整改/任务包整理＝**Claude 4.8**；技术施工＝**Code**；**终验**＝**GPT V6**。

## 0. 单一真相源（最重要）
- 本项目真相只在 `G:\我的云端硬盘\AI_Investment_System\00_请先看这里\` 的正式文件里。
- **开工先读** `★开工必读_主控文件.html`，再读本文件。聊天/记忆只是缓存，与文件冲突以文件为准。
- 干完必把结果回写文件，不许只留在对话里。

## 1. 你只负责什么
- 改代码、写脚本、灌数据、跑 py_compile/测试、组装HTML。
- **不设计、不决策、不改"尺"**（世界观/总则/过滤规则/框架只有董事长决策才动）。
- 收到的任务来自 **GPT V6 签发**或 **Opus 5** 提出的技术需求（由 Claude 4.8 整理成任务包），带【总则对照】；照做，不自作主张改结构。

## 1.5 存储分工与同步（必守）
- **尺+代码 → GitHub 主**（宪法/总则/元制度/右栏6块/开工必读/CLAUDE.md/scripts）：要版本历史、可回退。冲突以 GitHub 为准。
- **数据+每日输出 → G盘 主**（求证表/持仓/价格/记分卡 json、半成品/日报 html）：天天变、量大。冲突以 G盘 为准。
- **你的同步责任**：每次改完"尺"或代码，**顺手推 GitHub**（git add / commit / push）；别让 GitHub 过期。定期再批量同步一次当兜底。
- GitHub：github.com/zhuzhiqiang212-rgb/AI-Investment-System-Rules

## 2. 铁律（每次都守）
- **UTF-8、无乱码**：中文易乱码。每次写完文件**自己做字节级核验**（`EF BF BD` 计数=0），**不许假报"干净/完成"**。报告要能被独立复核。
- **全用 .html**：给董事长看的一律 html（董事长打不开 .md）。
- **文件唯一性**：一种性质一个正式版，改=**覆盖**不堆版本；废弃文件移进 `_历史归档`；**删文件要董事长签字**。
- **只读不下单**：不实际下单、不动 OpenD、不跑日报（除非任务明确要求）。
- **不碰**：V0、宪法、总则、准绳、记分卡——除非任务明确指定。
- **动静分清**：动态决策不写死进静态文件；只锚定义不锚具体标的死名单（总则第六条）。

## 2.6 生产=实时数据（董事长2026-07-15拍板·2026-07-17强化为明文铁律）

> ### ★铁律（无例外·不接受任何理由）
> **凡需董事长验收的完整产品，必须当日实时扫描新数据生产。**
> **旧数据配分析，不得当成品交验收。**
>
> #### ★★"当日"= 验收当天，机器可核三项（2026-07-17 董事长拍板·强化）
> 交验收前必须**逐项核过**，三项**全部**等于验收当天才算数：
> 1. **五册页头 `data_date` = 验收当天**；
> 2. **`run_id` 时间戳 = 当天**；
> 3. **`production` 快照 = 当天**。
>
> **旧数据重渲不算生产**：run_id 非当天、或页头横幅标了「N天前的·不是今天」的册，
> **一律不得当验收成品交**——那只能叫**整改自测件**，报告里必须写明是自测件。
> 实时源连不上 → **如实报「连不上·未生产」**，严禁旧数据顶充。
>
> 判定标准（机器可核，不靠自觉）：
> - 产品页头必须有 **data_date（数据日）+ 生产时间 + run_id** 的**超大横幅**，一眼可辨新旧；
> - 五册的 data_date / run_id **必须完全一致**（出厂 lint `L2 同源` 硬闸，不一致即不出品）；
> - `R3 快照单调闸`：本次 production 快照早于上次 → **拒绝生成**，防旧快照顶充；
> - 出厂后由 `product_manifest.py` 登记各册指纹；开工跑 `--check` 比对，
>   对不上 → 报「疑似被旧版覆盖」，**不许拿那份当今天的产品**。
>
> **实时源连不上时的唯一正确动作**：如实报「连不上·未生产」，**不许拿旧数据顶充硬跑**。
> 报「没做到」不扣分，**假报「做到了」是信任击穿**。

- **任何"生产/试生产/出正式产品"一律基于当日实时数据**。拿旧扫描数据配上分析≠生产，**不许当"今天的正式产品"交**；产品显要处必须标清数据真实日期/生成时间（data_date），不许含糊。
- **Opus 5（每日正式产品主脑；材料整理由 Claude 4.8 协助）的义务**：一旦要生产，**主动产出一份可直接复制转发的《机器侧生产扫描工单》**（写明日期、要跑的脚本、要出的产物、回报要求）交董事长转达 Code。**绝不拿"我连不上 OpenD／不能跑实时扫描"当理由搪塞董事长**——那是既定分工，不是每次的借口；我的活是把工单递出去，不是解释我为什么做不了。
- **实时数据来源**：机器侧(Code)在本机连 OpenD 取实时价 + 抓当日新闻 + 跑 production_pipeline → 出 `production_{当日}.json`、`evidence_chain/daily_{当日}.json` 等。这是 Code 的活。
- **固定顺序（缺①不得进②③）**：①Code 扫描出当日真数据 → ②分析岗写 `data/analysis/deep_{当日}.json`（按《完整产品_深度标准_v1》）→ ③`product_render_v3.py` 渲染出深度版 → ④董事长验收。
- 可复制工单模板见 `00_请先看这里/派工单_机器侧_每日生产扫描.html`。

## 2.7 每日自动生产（董事长2026-07-17拍板：不该靠他每天提才扫）

**已上线**：Windows 任务计划 `AI投资系统_每日自动生产`，**每天 07:30 JST 自动跑**，无需任何人点。
- 入口：`scripts/daily_auto_produce.py`（13 步：富途实时持仓→持仓真表→审持仓→production→均线→估值→当日涨跌→当日新闻→研报佐证→记分卡→复盘→三件魂→渲五册）
- 出品：当天五册（页头「今天的」·run_id/data_date=当天）+ 自动归档非当天的册到 `00_请先看这里/_历史归档/每日产品/`
- 台账：`data/logs/auto_produce_runs.json`；同时自动回写主控文件的「每日自动生产」块

### 怎么改跑的时间（董事长随时可调）
```
schtasks /Change /TN "AI投资系统_每日自动生产" /ST 08:00     # 改成 08:00
schtasks /Query  /TN "AI投资系统_每日自动生产"               # 看当前设置
schtasks /Run    /TN "AI投资系统_每日自动生产"               # 立刻手动跑一次
schtasks /Delete /TN "AI投资系统_每日自动生产" /F            # 停掉自动
```
或跑 `python scripts/daily_auto_produce.py --install --time 08:00` 重装。
**为什么默认 07:30**：日股开盘（09:00）前、美股昨夜已收 → 董事长早上打开就能看到当天的。

### 失败怎么办（铁律落地）
- **关键环失败**（OpenD 连不上／持仓拉不到／production 挂）→ 立即停、**当天不出品**，台账与主控记「未生产·原因」，**目录里不留旧版冒充今天**。
- **出厂 lint FAIL** → 旧册不被覆盖，记错因。
- 修好后 `schtasks /Run /TN "AI投资系统_每日自动生产"` 补跑即可。
- ⚠ 前提：**OpenD 客户端要开着**，任务计划只在用户已登录时跑。

## 3. 交付后的验收分层（2026-07-28 六岗位）
你的技术产出先由 **Claude 4.8（技术结果初验）**用"七清单"独立核（涉及正式产品内容/投资判断/预测的，再经 **Opus 5 产品初验**、**GPT V6 终验**）：①独立读实物②核真数据③验动态(改数据输出跟着变)④无乱码字节核⑤对照总则六条⑥结构完整⑦对照蓝图逐条查完整性。**所以别偷工、别假报**——会被逐条查出来。

## 4. 大白话
给董事长看的文字：无术语，四步=事实→为什么→对你影响→怎么办。

## 5. 关键位置
- scripts/：full_product_render(总组装)、production_pipeline(三关+护城河+机会池)、pdca_scorecard/pdca_review、moat_analysis、skill_gate、daily_startup_run、realtime_price
- **出厂三道闸（2026-07-17 加·每犯一类新错就加一条规则）**：
  - `deep_render.py` — 五册渲染器；出厂前**自动**跑 lint，FAIL 即**不出品**（旧册不被覆盖）
  - `product_lint.py` — 机械核 L1乱码/L2同源/L3转义渣/L4错模板/L5数字打架/L6状态词唯一/L7半截标题日期/L8新闻链接
  - `product_manifest.py` — 丙2防回滚哨兵（`--check` 比对G盘实物指纹）+ 丙3主控进度自动回写
- **产品命名**：`★每日产品_YYYY-MM-DD_序号_册名.html`（★置顶·同日五册按名连续相邻）
- data/：evidence_chain/、reports/、pdca/、moat/、accounts/、holdings/、valuation/、market/
- 纲领/尺：00_请先看这里/ 下各 .html
- GitHub：github.com/zhuzhiqiang212-rgb/AI-Investment-System-Rules

## 5.4 ★闸的判定必须基于结构化字段，不得靠自由文本关键词（裁定EA4·2026-08-02·第三次同型故障后立）

> **规矩**：**闸（gate）的判定依据必须是结构化字段（布尔/日期/数值/枚举），不得依赖自由文本（`_说明`/`detail`/`note`/`reason`/`上一版处置`等）里的关键词匹配。确需读文本时，必须能可靠区分【主语与指代对象】（"本清单作废" vs "描述上一版作废"）；做不到可靠区分就不判。**

**为什么**：闸只认关键词/形式、不认语义/上下文，已连续三次酿祸——
1. **vintage 闸**误读 `val_inputs` 的 `intentional_conservative`（有意保守被判陈旧）→ 软银当盲区 121 天；
2. **scan_date 闸**时间无关（收盘后仍按未收盘判）→ 生产卡死在必然失败循环；
3. **激活清单新鲜度闸**把「本文件描述【上一版】作废」读成「本文件自己作废」+ `sorted(glob)` 选中 `TEMPLATE` → 候选 0（轮87）。

**落地**：作废/失效判定只看显式字段（`"作废": true` / `"有效期至"` / `data_date` 数值比较）；**非当日 ≠ 作废**（周末/off-cycle 接受最近有效清单）；**TEMPLATE 等模板文件永不得被当作实际数据读取**（选文件按 `data_date` 最新且非模板，不用 `sorted(glob)[-1]`）。改闸/建闸前对照本条自查。

## 5.5 数据坑位（踩过的·不写会重踩·2026-07-21 汇入）

- **field_id 两套不能混**：美股/IFRS 用 **8xxx**（8001营收 8003成本 8004毛利 8015 OCF 8017营业利润 8034税前 8035所得税 8037净利）；日股本土准则用 **11xxx**。**混用会串号**，苹果+丰田交叉验证过。8033 当 capex 对苹果合理、对丰田不合理 → **标「待查·不采信」，不要估算放行**。
- **可用于"无未来信息回测"的柱子 = 0 根**：statements 只返回**当前值**，无历史快照，排不掉重述。→ **回测目前只能用「K线 + 财报发布日」，不能用财务数值。**
- **「象限分歧 false」≠「四项一致向好」**：89 只里真正四项一致的只有 39 只。判定要显式核四项，不能只看分歧标志位。
- **VST/NRG/CEG 不可整只合并看**：NRG 毛利率 13.6% 是**零售商属性**，不是电厂。同板块不等于同商业模式。
- **小样本 <10 不下结论**；**毛利率必须用 ANNUAL 口径**，取到 **0.0 一律转 null**（0.0 是缺失不是真值）。
- **成交额门槛要用 60 日均，不能用当日近似**：曾因此把 BWXT($203M)/NRG($419M) 误判落选。
- **硬门槛上线前必须先验数据源可得率**：净负债设成硬门槛而未验证 → 16,763 只扫出 0 入围。
- **★数据层文案不许夹带 HTML 标签（裁定J4·2026-07-27）**：数据层（`evidence_autobuild` 等写 data/*.json 的脚本）的文案字符串里**不许写 `<b>`/`<br>`/`<span>` 这类样式标签**——渲染层 `esc()` 会把它转成字面量 `&lt;b&gt;` 印给董事长（L3 转义渣）。这类只在特定分支触发（如 `<b>先不翻</b>` 只在阻尼regime那天暴露·07-22 埋下 07-27 才炸），**会反复发作**。铁规矩：**强调用「」，样式归渲染层，数据层不带样式**。已加 `data_html_leak_gate.py`（daily_auto_produce ⑦c·非关键只告警）扫数据层脚本+当日数据·行内 `# html-ok` 可豁免。

## 6. 当前阶段
**D阶段·组装完整产品。**

> **【主语说明·2026-07-28 六岗位】** 本节「架构师」：现行规划/正式内容/投资判断/产品初验＝**Opus 5**（材料整理/规则预检＝**Claude 4.8**）；任务签发/最终验收/验收后下一步裁定＝**GPT V6**；技术施工＝**Code**。凡标「曾／已」的架构师动作为**历史记录**，不作现行指令。

**⚠ 2026-07-21 方向纠偏（董事长点破）**：架构师曾绕过《右栏_过滤标准筛选规则》第 1 关，用全市场财务扫描自下而上出候选，违反总则第一条。**该批产物（尺总册v1／总则第十五条1~10版／第十六条提案／两日筛选设计稿约20份）已经董事长签字废止**，移入 `_历史归档/20260721_废止_装反漏斗产物`。**432 只候选池未过第 1 关，不采用。**

**正确顺序（既有框架，不是新规）**：世界观 → 战略地图 → 资金流 → 板块地图 → **第1关激活板块（一票否决）** → 第2关软性 → 第3关估值 → 第4关护城河 → 第5关个股研究。**财务扫描属第2~3关之间的工具，不是漏斗本身。**

### ★活看板（**Opus 5**（每日正式产品主脑）每轮开头先读这里，别再问董事长"下一步做什么"·2026-07-22 立·材料整理由 Claude 4.8）

> **Opus 5 自查（每轮回话前默做，回话开头用一句报「上步完成X·本步做Y」，让董事长一眼可验；材料整理/规则预检由 Claude 4.8）**：
> ①上一步交付了什么？②本步按看板该做哪一件？③改完 JSON/文件后，回扫自检段与全文，有没有被带出数字打架/过期文字？（连续两次验收都栽在这第③条）
>
> **★交付形式铁律（董事长2026-07-22·烦过两次）**：凡要董事长转给 Code / GPT 的东西，一律在聊天里给可整块复制的```代码块```，框选即走。**绝不塞进 HTML 让董事长去翻**。HTML 只作存档留底供 PDCA，不作复制源。
>
> **★「去G盘验收」的定义（董事长2026-07-22·澄清）·现行主语＝Claude 4.8（技术结果初验）／Opus 5（产品初验）**：「去G盘验收」＝**验 Code 交付的产出**（`data/` 下 Code 生成的 json/轨迹/快照等），**不是验规划岗自己改的尺文件**。验收动作＝独立读 Code 的实物→核真数据/时间戳(data_date·run_id=当天)/字节乱码→对照派工单逐项核做没做到→用七清单精神挑问题→回报。**【历史】原架构师岗曾连续把"去G盘验收"错做成"验自己改的文件"，导致重复发派工单、看不见 Code 已完成的活。此后一律先进 `data/` 验 Code（现行：4.8 技术初验／Opus 5 产品初验）。**
>
> **★验收后必接下一步指令（董事长2026-07-22）**：①每次验收 Code 完，**GPT V6（任务签发／验收后下一步裁定）必须紧接着给出下一个任务指令**，不等董事长问「任务呢」。②该指令**必须是聊天里可整块复制的```代码块```**（对应上条交付形式铁律），董事长框选即转 Code。**验收→给下一条可复制指令，是一个连续动作，不许在验收后停下。**
>
> **★验Code产出用Glob/Read必须带G盘完整path（董事长2026-07-22·误判Code三次的教训）**：`data/news/*.json` 这种相对pattern会被解析到本地outputs目录、查不到G盘文件，导致误报「Code没做」。查G盘一律带 path 指向 `G:\...\AI_Investment_System\...`（现行验 Code 产出者＝Claude 4.8 技术初验／Opus 5 产品初验）。【历史】原架构师岗曾因此连续三轮误判「新闻留档没做」并错怪Code——实际文件一直在 data/news/。**报「Code没做」前先自查是不是自己路径查错。**

**已完成（2026-07-22）**：D-06 板块地图接通（`data/market/sector_activation_20260722.json`·**18格**全核·含资金流regime层·**已补第18格「安全/国防」战略主线**）｜财务扫描装回第2关当「谁是龙头」工具｜GPT四条验法落地第1关出厂验收｜Code已交B1账户快照(仓位91.6%/防御仓8.4%破15%下限/日股45.3%破30线)+gate1_trace(432→193过·金矿全挡·礼来进受检·漏斗通过)｜crosswalk核后发现安全线漏格已补·SpaceX归位太空节点。

**下一步（Opus 5 规划·按序·不必问董事长）**：
1. **落实 GPT 四条验法**到第1关验收标准（入口定位/逐关轨迹/反向样本金矿必挡·礼来必进/时间戳顺序）——这是纯设计活，不等 Code。
2. 等 Code 交 B1 账户快照 → 才能算 几成仓/日股是否超单一驱动30%/集中度，届时出配仓体检。
3. 7/29 FOMC 后 regime 变 → 激活清单作废重出。
4. D1：右栏6块尺接进左栏生成器(full_product_render 读右栏底子填右栏占位)。详见 完整产品执行计划.html。

**★里程碑（2026-07-22）**：漏斗**首次端到端跑通至第4关**。世界观→战略→resime→18格激活→第1关(432→216)→第2关四象限(真主池15)→第3关估值(过7)→第4关护城河(7只五维)。结论：**7只无「宽护城河+便宜」完美组合**(KLAC宽但估值合理/GSK便宜但护城河窄)——不勉强出手。配仓体检：仓位92.2%/防御仓7.8%(破15%下限)/日股46%(破30线)/单只max MSFT16.4%(未破20)。

> **★防御下限口径澄清（裁定I1·2026-07-27）**：防御仓下限＝**15%**（防御股占比·`CONC_LOWER_LIMITS`＝生产规范尺＋代码＋production 自07-17一致·**作数**）。此前"破20线"记述有误——20% 出自《废止与分流执行单_20260721》且带"**不含股票**"限定，量的是现金+债这类防守垫，**与防御股占比是两个口径·不能混用·20那条已不作数**。正式尺《取消45上限改风险配仓》未含防御条文。

**⏳ 等 Code 交（第五轮·正在跑）**：F1补portfolio_check的SpaceX(缺第20只)｜F2确认gate3 json.load｜F3推GitHub。

**★里程碑2（2026-07-22）**：第一批架构师预测已 forecast_lock 正式锁定，**PDCA 第一次开始计时**。4条(软银短/长·爱德万短/长)进记分卡，SHA256+Google外部时间戳(14:11·晚于新闻源=防事后编)。核对日：爱德万短8/5·软银短8/19·爱德万长10/31·软银长2027-1/22。第3关已升级双时间尺度预判+驱动分流按类型(软银修正为控股/NAV驱动)。

**★里程碑3（2026-07-22）**：主线三大件全闭环。①**基线锁定**：35只(实际53条含v2)双时间尺度预测 forecast_lock·补锁5条(CRCL/SNDK/丰田长期55%被三铁律拒→改押方向;万代/任天堂短期→不锁)·登记记分卡(短25/长29进分母)·PDCA计时开始。②**合main收口**：基线+尺+代码合进GitHub main(c073bb7→e33f69f→6c1ca6e)·两处漂移(主控.html/full_product_render.py)worktree零风险收口·main不再过期。③**护城河公用评法**：通用五维对受监管公用系统性失灵→架构师增补《护城河分析框架.html》公用五维(监管特许/高效规模/受监管回报/资产壁垒/需求刚性)+天花板注(宽护城河=收息防御·非成长)·D/PEG从「无护城河」纠为「宽护城河」·gate4_moat.py接码(band_util·③待核不计分)。

**★Opus 5 的下一步（不等Code）**：①✅铺开全持仓+候选逐只填预测+锁定(=里程碑2/3基线锁定，已完成) ②✅公用事业护城河专用评法(已完成，见里程碑3③)。→ 主线待推：D1右栏6块尺接左栏生成器(下一步4)；7/29 FOMC后regime变、激活清单重出。

**老挂账**：✅**P1催化剂库来源三要素**(已落地·尺《催化剂库_来源三要素规范_v1.html》+data/catalyst/catalyst_library.json 5条+event_driven接库+7条预测引用CAT-ID) ｜⚠**P2三条老挂账/P3董事长当面提的三处界面**——翻遍.md档案仅CLAUDE.md本行一句简记·当时未落档细节·**细节遗失·待董事长重述再做**(Opus 5／Claude 4.8均不得凭空猜测·防像7-21那样跑偏)。

**开工前必读顺序**：`★开工必读_主控文件.html` → 本文件 → **《右栏_过滤标准筛选规则》与《右栏_板块地图》**（任何涉及"挑标的/筛选/候选池"的任务，这两份是硬前置，不读不许动手）。
