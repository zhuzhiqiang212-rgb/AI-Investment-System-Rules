# CLAUDE.md — AI投资决策系统 · 工程师(Code/执行)守则

> 本文件是执行工具（Code/Codex）每次开工自动读的规矩。你的角色是**工程师**：只改文件/写脚本，**不做设计、不做决策、不做验收**。设计和决策由 Chat(首席架构师) 定，你只执行。

## 0. 单一真相源（最重要）
- 本项目真相只在 `G:\我的云端硬盘\AI_Investment_System\00_请先看这里\` 的正式文件里。
- **开工先读** `★开工必读_主控文件.html`，再读本文件。聊天/记忆只是缓存，与文件冲突以文件为准。
- 干完必把结果回写文件，不许只留在对话里。

## 1. 你只负责什么
- 改代码、写脚本、灌数据、跑 py_compile/测试、组装HTML。
- **不设计、不决策、不改"尺"**（世界观/总则/过滤规则/框架只有董事长决策才动）。
- 收到的任务来自 Chat，带【总则对照】；照做，不自作主张改结构。

## 1.5 存储分工与同步（必守）
- **尺+代码 → GitHub 主**（宪法/总则/元制度/右栏6块/开工必读/CLAUDE.md/scripts）：要版本历史、可回退。冲突以 GitHub 为准。
- **数据+每日输出 → G盘 主**（求证表/持仓/价格/记分卡 json、半成品/日报 html）：天天变、量大。冲突以 G盘 为准。
- **你的同步责任**：每次改完"尺"或代码，**顺手推 GitHub**（git add / commit / push）；别让 GitHub 过期。定期再批量同步一次当兜底。
- GitHub：github.com/zhuzhiqiang212-rgb/AI-Investment-System-Rules

## 2. 铁律（每次都守）
- **UTF-8、无乱码**：中文易乱码。每次写完文件**自己做字节级核验**（`EF BF BD` 计数=0），**不许假报"干净/完成"**。报告要能被独立复核。
- **全用 .html**：给董事长看的一律 html（董事长打不开 .md）。
- **文件唯一性**：一种性质一个正式版，改=**覆盖**不堆版本；废弃文件移进 `_历史归档`；**删文件要董事长签字**。
- **只读不下单**：不实际下单、不动 OpenD、不跑日报（除非任务明确要求）。
- **不碰**：V0、宪法、总则、准绳、记分卡——除非任务明确指定。
- **动静分清**：动态决策不写死进静态文件；只锚定义不锚具体标的死名单（总则第六条）。

## 2.6 生产=实时数据（董事长2026-07-15拍板·2026-07-17强化为明文铁律）

> ### ★铁律（无例外·不接受任何理由）
> **凡需董事长验收的完整产品，必须当日实时扫描新数据生产。**
> **旧数据配分析，不得当成品交验收。**
>
> #### ★★"当日"= 验收当天，机器可核三项（2026-07-17 董事长拍板·强化）
> 交验收前必须**逐项核过**，三项**全部**等于验收当天才算数：
> 1. **五册页头 `data_date` = 验收当天**；
> 2. **`run_id` 时间戳 = 当天**；
> 3. **`production` 快照 = 当天**。
>
> **旧数据重渲不算生产**：run_id 非当天、或页头横幅标了「N天前的·不是今天」的册，
> **一律不得当验收成品交**——那只能叫**整改自测件**，报告里必须写明是自测件。
> 实时源连不上 → **如实报「连不上·未生产」**，严禁旧数据顶充。
>
> 判定标准（机器可核，不靠自觉）：
> - 产品页头必须有 **data_date（数据日）+ 生产时间 + run_id** 的**超大横幅**，一眼可辨新旧；
> - 五册的 data_date / run_id **必须完全一致**（出厂 lint `L2 同源` 硬闸，不一致即不出品）；
> - `R3 快照单调闸`：本次 production 快照早于上次 → **拒绝生成**，防旧快照顶充；
> - 出厂后由 `product_manifest.py` 登记各册指纹；开工跑 `--check` 比对，
>   对不上 → 报「疑似被旧版覆盖」，**不许拿那份当今天的产品**。
>
> **实时源连不上时的唯一正确动作**：如实报「连不上·未生产」，**不许拿旧数据顶充硬跑**。
> 报「没做到」不扣分，**假报「做到了」是信任击穿**。

- **任何"生产/试生产/出正式产品"一律基于当日实时数据**。拿旧扫描数据配上分析≠生产，**不许当"今天的正式产品"交**；产品显要处必须标清数据真实日期/生成时间（data_date），不许含糊。
- **架构师/分析岗(Chat)的义务**：一旦要生产，**主动产出一份可直接复制转发的《机器侧生产扫描工单》**（写明日期、要跑的脚本、要出的产物、回报要求）交董事长转达 Code。**绝不拿"我连不上 OpenD／不能跑实时扫描"当理由搪塞董事长**——那是既定分工，不是每次的借口；我的活是把工单递出去，不是解释我为什么做不了。
- **实时数据来源**：机器侧(Code)在本机连 OpenD 取实时价 + 抓当日新闻 + 跑 production_pipeline → 出 `production_{当日}.json`、`evidence_chain/daily_{当日}.json` 等。这是 Code 的活。
- **固定顺序（缺①不得进②③）**：①Code 扫描出当日真数据 → ②分析岗写 `data/analysis/deep_{当日}.json`（按《完整产品_深度标准_v1》）→ ③`product_render_v3.py` 渲染出深度版 → ④董事长验收。
- 可复制工单模板见 `00_请先看这里/派工单_机器侧_每日生产扫描.html`。

## 2.7 每日自动生产（董事长2026-07-17拍板：不该靠他每天提才扫）

**已上线**：Windows 任务计划 `AI投资系统_每日自动生产`，**每天 07:30 JST 自动跑**，无需任何人点。
- 入口：`scripts/daily_auto_produce.py`（13 步：富途实时持仓→持仓真表→审持仓→production→均线→估值→当日涨跌→当日新闻→研报佐证→记分卡→复盘→三件魂→渲五册）
- 出品：当天五册（页头「今天的」·run_id/data_date=当天）+ 自动归档非当天的册到 `00_请先看这里/_历史归档/每日产品/`
- 台账：`data/logs/auto_produce_runs.json`；同时自动回写主控文件的「每日自动生产」块

### 怎么改跑的时间（董事长随时可调）
```
schtasks /Change /TN "AI投资系统_每日自动生产" /ST 08:00     # 改成 08:00
schtasks /Query  /TN "AI投资系统_每日自动生产"               # 看当前设置
schtasks /Run    /TN "AI投资系统_每日自动生产"               # 立刻手动跑一次
schtasks /Delete /TN "AI投资系统_每日自动生产" /F            # 停掉自动
```
或跑 `python scripts/daily_auto_produce.py --install --time 08:00` 重装。
**为什么默认 07:30**：日股开盘（09:00）前、美股昨夜已收 → 董事长早上打开就能看到当天的。

### 失败怎么办（铁律落地）
- **关键环失败**（OpenD 连不上／持仓拉不到／production 挂）→ 立即停、**当天不出品**，台账与主控记「未生产·原因」，**目录里不留旧版冒充今天**。
- **出厂 lint FAIL** → 旧册不被覆盖，记错因。
- 修好后 `schtasks /Run /TN "AI投资系统_每日自动生产"` 补跑即可。
- ⚠ 前提：**OpenD 客户端要开着**，任务计划只在用户已登录时跑。

## 3. 交付后会被 Chat 用"七清单"验收
你的产出会被独立核：①独立读实物②核真数据③验动态(改数据输出跟着变)④无乱码字节核⑤对照总则六条⑥结构完整⑦对照蓝图逐条查完整性。**所以别偷工、别假报**——会被逐条查出来。

## 4. 大白话
给董事长看的文字：无术语，四步=事实→为什么→对你影响→怎么办。

## 5. 关键位置
- scripts/：full_product_render(总组装)、production_pipeline(三关+护城河+机会池)、pdca_scorecard/pdca_review、moat_analysis、skill_gate、daily_startup_run、realtime_price
- **出厂三道闸（2026-07-17 加·每犯一类新错就加一条规则）**：
  - `deep_render.py` — 五册渲染器；出厂前**自动**跑 lint，FAIL 即**不出品**（旧册不被覆盖）
  - `product_lint.py` — 机械核 L1乱码/L2同源/L3转义渣/L4错模板/L5数字打架/L6状态词唯一/L7半截标题日期/L8新闻链接
  - `product_manifest.py` — 丙2防回滚哨兵（`--check` 比对G盘实物指纹）+ 丙3主控进度自动回写
- **产品命名**：`★每日产品_YYYY-MM-DD_序号_册名.html`（★置顶·同日五册按名连续相邻）
- data/：evidence_chain/、reports/、pdca/、moat/、accounts/、holdings/、valuation/、market/
- 纲领/尺：00_请先看这里/ 下各 .html
- GitHub：github.com/zhuzhiqiang212-rgb/AI-Investment-System-Rules

## 6. 当前阶段
D阶段·组装完整产品。下一步 D1：把右栏6块尺接进左栏生成器(full_product_render 读右栏底子填右栏占位)。详见 完整产品执行计划.html。
