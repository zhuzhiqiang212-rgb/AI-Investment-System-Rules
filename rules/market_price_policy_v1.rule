# Market Price Policy V1

## 1. 目标

将市场价格口径正式写入 AI投研系统 V4，确保 Claude、Codex、Cursor、ChatGPT / AI投研总控台 V4 对价格时效、价格命名、使用边界保持一致。

## 2. Rule-MP001：默认市场价格规则

| 字段 | 规则 |
|---|---|
| 默认市场价格 | 延迟行情 |
| 延迟估计 | 15~20分钟 |
| 价格用途 | 研究参考、日报判断、风险温度、价格区间参考、PDCA复核 |
| 禁止用途 | 自动下单、自动买卖、自动生成交易指令、替代券商实时界面 |

## 3. 适用市场

本规则适用于：

- 美股
- 日股
- A股
- ETF
- 加密资产

## 4. 适用 Worker

本规则适用于：

- Claude
- Codex
- Cursor
- ChatGPT / AI投研总控台 V4

所有 Worker 在报告、验收包、预检、OCR、工程脚本、日报、账户动作卡中都必须使用同一价格口径。

## 5. 允许使用的价格表述

以下表述允许使用：

| 允许表述 | 含义 |
|---|---|
| 参考价 | 非实时、仅用于研究参考 |
| 延迟行情价 | 默认 15~20 分钟延迟行情 |
| 最近可获得价格 | 当前系统能读到的最近价格，不代表实时成交 |
| 最近市场价格 | 当前可获得市场报价，不代表实时盘口 |
| 最近收盘价 | 最近一个完整交易日收盘价 |

## 6. 禁止使用的价格表述

除非系统明确接入实时付费行情源，并通过 ChatGPT / AI投研总控台 V4 验收，否则禁止使用：

- 实时价格
- 当前成交价
- 即时成交价
- 实时盘口
- 最新成交价
- 盘口买一/卖一
- 可立即成交价格

如果第三方资料出现这些词，进入系统时必须降级为“来源文本原话，不代表系统已接入实时行情”。

## 7. 账户规则

账户快照 * 延迟15~20分钟行情 = 研究参考。

解释：

- 账户快照可能有时间差。
- 延迟行情也有时间差。
- 两者叠加后，只能用于研究参考、风险温度和条件式观察。
- 不能作为自动交易依据。

## 8. 执行边界

禁止：

- 自动下单
- 自动生成交易指令
- 自动执行买卖
- 自动挂单
- 自动撤单
- 用延迟行情替代券商实时界面
- 用账户快照替代实时持仓界面

## 9. 强制人工确认语句

所有执行建议、价格指导、账户动作卡、短线价格卡、日报结论中，只要涉及可能的加仓、减仓、买入、卖出、挂单、撤单、风险调整，都必须保留以下语句：

> 实际交易前请人工确认券商界面价格、持仓、现金、挂单。

如涉及保证金、信用、现物账户，还必须补充：

> 同时确认保证金/信用/现物状态、账户成本和可用现金。

## 10. 状态冲突处理

| 冲突 | 处理 |
|---|---|
| Worker 写“实时价格”但无实时源 | 改为“延迟行情价/参考价”，并标注不可直接交易 |
| 报告写“最新成交价”但来源是公开延迟行情 | 改为“最近可获得价格” |
| 短线价格指导没有人工确认提醒 | 任务不通过，必须返工 |
| 账户动作暗示自动交易 | 任务不通过，必须返工 |
| 实时付费行情源未验收却被使用 | 状态冲突，必须回滚 |

## 11. 与其他治理层关系

- 继承 Worker Governance Layer V1 的角色分离原则。
- 继承 Validation Workflow Layer V1 的验收包和最终验收规则。
- 继承 Account Freshness Layer V1 的账户快照新鲜度边界。
- 最终是否允许写回绿色，只能由 ChatGPT / AI投研总控台 V4 验收决定。
