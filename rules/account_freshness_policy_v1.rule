# Account Freshness Policy V1

## 1. 目标

修正账户新鲜度规则：账户快照新鲜度不再简单按 7 天判断，而以“账户是否发生持仓、现金、挂单或交易变化”为核心判断。

本规则只定义账户数据使用边界，不生成日报、不修改账户数据、不修改投资结论。

## 2. Rule-AF001：账户变化驱动规则

账户快照新鲜度以“账户是否发生变化”为核心判断，而不是单纯按日期判断。

如果账户未发生以下变化，则最近一次人工确认账户快照仍可作为结构参考：

- 持仓变化
- 现金大额变化
- 挂单变化
- 新增买入
- 卖出
- 转入资金
- 转出资金

## 3. 状态定义

| 状态 | 定义 | 用途边界 |
|---|---|---|
| CURRENT_CONFIRMED | 账户已由用户确认，且用户说明近期无交易 / 无明显变化 | 可作为仓位结构、风险暴露、执行卡条件、持仓映射的结构参考 |
| NEEDS_CONFIRMATION | 超过30天未确认，或市场波动巨大，或系统需要精确执行卡 | 需要用户确认账户是否有变化；不能输出精确数量/金额动作 |
| CHANGED_UPDATE_REQUIRED | 用户发生交易、转账、挂单变化、持仓变化 | 必须更新账户截图或账户快照后才能用于执行卡 |
| MISSING | 没有账户数据 | 不得输出账户级确定动作 |

## 4. 账户数据用途

账户数据仅用于：

- 仓位结构参考
- 风险暴露参考
- 执行卡条件参考
- 持仓映射参考

账户数据禁止用于：

- 自动下单
- 精确交易
- 未经人工确认的买卖数量
- 未经确认的实时账户价值判断
- 直接替代券商实时界面
- 直接替代用户人工确认

## 5. 市场价格规则关系

账户快照 * 延迟15~20分钟市场价格 = 投研参考。

解释：

- 市场价格统一遵循 Market Price Policy V1：默认延迟行情，约15~20分钟。
- 账户快照是否可用，取决于账户是否发生变化，而不是价格是否延迟。
- 账户快照不需要每天上传，除非账户发生交易、转账、挂单、持仓变化，或系统需要精确执行卡。

## 6. 禁止混淆

禁止把以下两件事混为一谈：

| 概念 | 正确解释 |
|---|---|
| 账户快照过期 | 账户数据是否需要用户重新确认，核心看账户是否变化 |
| 市场价格延迟 | 市场报价默认延迟15~20分钟，核心看价格源时效 |

错误写法：

- “账户截图超过7天，所以账户数据不可用。”
- “市场价格延迟，所以账户快照也必须当天更新。”
- “账户快照旧，所以可以忽略已有持仓结构。”

正确写法：

- “账户快照已由用户确认且近期无交易，可作为结构参考。”
- “若账户发生买卖、转账或挂单变化，必须更新账户快照。”
- “实际交易前仍必须人工确认券商界面价格、持仓、现金、成本、挂单。”

## 7. 执行卡使用规则

| 场景 | 账户状态 | 执行卡处理 |
|---|---|---|
| 用户确认近期无交易 | CURRENT_CONFIRMED | 可引用持仓结构和风险暴露，但保留人工确认提醒 |
| 超过30天未确认 | NEEDS_CONFIRMATION | 执行卡写“需确认账户是否变化” |
| 用户发生交易/转账/挂单变化 | CHANGED_UPDATE_REQUIRED | 执行卡不得使用旧快照输出确定动作 |
| 没有账户数据 | MISSING | 不得输出账户级确定动作 |

## 8. 强制人工确认语句

任何涉及账户动作、买卖、加仓、减仓、挂单、撤单、风险调整的内容，都必须保留：

> 实际交易前请人工确认券商界面价格、持仓、现金、挂单。

如涉及信用/保证金/现物账户，还必须补充：

> 同时确认保证金/信用/现物状态、账户成本和可用现金。

## 9. 对 Account Data Freshness Layer V1 的修正

Account Data Freshness Layer V1 中“7天内 = FRESH、超过7天 = STALE”的规则需要修正。

新规则：

- 不再以7天作为主判断。
- 以用户是否确认账户无变化、是否发生交易/转账/挂单/持仓变化作为主判断。
- 30天未确认或需要精确执行卡时，进入 NEEDS_CONFIRMATION。
- 发生账户变化时，进入 CHANGED_UPDATE_REQUIRED。

## 10. 最终验收边界

本规则由 Codex 创建，但不能由 Codex 最终验收或自动标绿。

最终验收人：ChatGPT / AI投研总控台 V4。
