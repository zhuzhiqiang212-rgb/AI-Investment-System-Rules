# Validation Workflow Layer V1

## 1. 目标

统一 Claude、Codex、Cursor 等 Worker 的任务交付、预检、最终验收、返工流转标准，解决任务完成后“验收包在哪里、给谁验收、谁负责反馈、谁负责返工”不统一的问题。

## 2. 适用对象

| Worker | 默认角色 | 说明 |
|---|---|---|
| Claude | 预检 | 负责长文、内容可读性、证据链、风险点、验收包预检；不能最终验收。 |
| Codex | 执行 | 负责系统任务执行、文件生成、规则/报告/验收包生成；不能最终验收。 |
| Cursor | 工程执行 | 负责 OCR、PDF处理、脚本、Dashboard、GitHub工程、本地代码工具；不能最终验收。 |
| ChatGPT / AI投研总控台 V4 | 最终验收 | 负责最终验收、决定是否标绿、是否返工。 |

## 3. 任何 Worker 完成任务后必须输出

以下字段为强制字段。缺任一字段，任务视为未完成。

```text
【状态】
PASS / PARTIAL / FAIL

【执行人】
Claude / Codex / Cursor

【预检人】
Claude / Codex / Cursor / 无

【最终验收人】
ChatGPT / AI投研总控台 V4

【发给谁】
ChatGPT / AI投研总控台 V4 / 指定预检Worker

【验收包路径】
完整 Windows 路径

【文件是否真实存在】
YES / NO

【文件大小】
字节数

【最后修改时间】
本地文件时间

【下一步】
一句话说明

【唯一动作】
只允许一个动作

【可直接执行指令】
用户或下一Worker可以直接复制执行的指令
```

## 4. 强制未完成规则

| 情况 | 判定 |
|---|---|
| 没有验收包路径 | 未完成 |
| 验收包文件不存在 | 未完成 |
| 验收包文件为空 | 未完成 |
| 没有发给谁 | 未完成 |
| 没有唯一动作 | 未完成 |
| 没有可直接执行指令 | 未完成 |
| 状态写 PASS 但文件不存在 | 未完成且状态冲突 |
| Worker 自己最终标绿 | 违反角色分离，必须返工 |
| 未经 ChatGPT / 总控台 V4 验收就写回绿色 | 违反验收流程，必须回滚 |

## 5. 默认流转规则

```text
Codex 执行任务
↓
Codex 生成原始验收包
↓
Claude 或右侧 Codex 做预检（如用户要求）
↓
用户上传同一个原始验收包给 ChatGPT / AI投研总控台 V4
↓
ChatGPT / AI投研总控台 V4 最终验收
↓
通过：允许写回绿色
不通过：返回修正指令给原执行Worker返工
```

## 6. 返工责任

| 问题类型 | 返工负责人 |
|---|---|
| 文件未创建、路径错误、格式错误 | 原执行Worker |
| 内容不完整、只有YES/NO、缺真实摘录 | 原执行Worker |
| OCR错误、PDF/图片无法解析 | Cursor |
| 长文理解、证据链、可读性预检不足 | Claude |
| 系统状态写回错误 | Codex |
| 最终验收标准不清 | ChatGPT / AI投研总控台 V4 给出明确修正指令 |

## 7. 禁止事项

- Worker 不得代替 ChatGPT / AI投研总控台 V4 做最终验收。
- Worker 不得在未验收前自行标绿。
- Worker 不得用预检 PASS 替代最终验收 PASS。
- Worker 不得没有验收包就声称任务完成。
- Worker 不得只输出路径而不确认文件存在、大小、修改时间。
- Worker 不得输出多个“下一步唯一动作”。
- Worker 不得在验收流转任务中修改账户数据、投资结论或执行交易动作。

## 8. 标准完成回复模板

```text
【状态】
PASS / PARTIAL / FAIL

【执行人】

【预检人】

【最终验收人】
ChatGPT / AI投研总控台 V4

【发给谁】

【验收包路径】

【文件是否真实存在】
YES / NO

【文件大小】

【最后修改时间】

【下一步】

【唯一动作】

【可直接执行指令】
```
