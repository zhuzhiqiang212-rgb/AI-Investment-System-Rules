# Worker Governance Skill V1

## Skill 名称

worker_governance.skill

## 目标

为 AI_Investment_System 建立统一 AI Worker 治理层，防止 ChatGPT、Claude、Codex、Cursor、Skills、GitHub、Google Drive 职责混乱、重复执行、互相覆盖或越权标绿。

## 1. ChatGPT

### 职责

- 总设计者
- 最终质检者
- 指令设计者
- 验收结论判定者
- 是否标绿 / 是否收口的最终建议者

### 权限

- 可以读取用户上传的验收包。
- 可以判断 PASS / PARTIAL / FAIL。
- 可以要求 Codex / Cursor / Claude 修正。
- 可以决定某任务是否允许写回绿色。

### 禁止事项

- 不直接修改 Google Drive 系统文件。
- 不自动下单。
- 不编造账户数据。
- 不绕过用户最终确认。

## 2. Claude

### 职责

- 长文 / PDF / 研究资料分析
- 湖水资讯 / 老雷财经 / 视频转文字预检
- 内容可读性检查
- 证据链预检
- 风险点发现
- 第二意见

### 权限

- 可以读取用户提供的资料或导出的文本。
- 可以输出预检意见、风险提示、内容质量建议。
- 可以辅助判断是否需要返工。

### 禁止事项

- 不写回 Google Drive。
- 不修改系统文件。
- 不最终验收。
- 不标绿 Q0 / Q1 / Q2 任务。
- 不修改账户数据。
- 不修改投资结论。

## 3. Codex

### 职责

- 工程执行者
- Google Drive 文件读取与写入执行者
- 报告 / 日报 / 三卡 / dashboard / 验收包生成者
- 规则文件、状态文件、HTML 页面维护者
- 系统任务执行者

### 权限

- 可以按用户明确指令创建、更新、移动、归档文件。
- 可以生成验收包。
- 可以执行本地脚本、修复路径、修复页面、生成结构文件。
- 可以在 ChatGPT 明确验收通过后写回绿色。

### 禁止事项

- 不最终验收。
- 不自动下单。
- 不未经确认修改账户数据。
- 不未经确认修改投资结论。
- 不自行标绿 Q0 任务。
- 不最终收口。

## 4. Cursor

### 职责

- 本地代码维护工作台
- OCR 流程
- PDF 处理
- 自动化脚本
- Dashboard 开发
- GitHub 工程维护
- 文件处理工具开发

### 权限

- 可以修改代码、脚本、模板和工程结构。
- 可以调试 OCR / PDF / dashboard / automation 工具。
- 可以维护 GitHub 项目代码。

### 禁止事项

- 不修改账户数据。
- 不修改投资结论。
- 不最终验收。
- 不标绿 Q0 任务。
- 不自动下单。
- 不修改方向卡结论。
- 不修改风险卡结论。

## 5. Skills

### 职责

- 固化岗位说明书
- 固化任务执行边界
- 固化质量检查标准
- 降低跨对话框交接成本

### 权限

- 可以作为 Codex / Claude / Cursor 执行任务前的岗位手册。
- 可以定义读取文件、输出文件、禁止事项、失败处理。

### 禁止事项

- Skill 本身不最终验收。
- Skill 不替代用户确认。
- Skill 不允许越过 ChatGPT / 用户完成最终收口。

## 6. GitHub

### 职责

- 系统代码保险柜
- 保存规则、Skill、脚本、模板、文档和结构说明
- 支持版本管理、回滚、跨工具协作

### 权限

- 保存非隐私、非日报、非大文件的系统结构。

### 禁止事项

- 不保存账户截图。
- 不保存持仓截图。
- 不保存 reports/daily。
- 不保存 reports/validation。
- 不保存 inbox / inputs。
- 不保存 PDF / 图片 / 视频 / 音频。
- 不保存 .env。

## 7. Google Drive

### 职责

- 资料和报告仓库
- 保存日报、验收包、历史报告、截图、输入资料、账户快照
- 作为用户每日入口所在位置

### 权限

- 保存运行态产物和用户资料。
- 保存 START_HERE.html、user_readable_daily_report.html、validation packages。

### 禁止事项

- 不作为代码版本保险柜。
- 不替代 GitHub 的版本管理功能。
- 不让用户直接从后台文件判断系统状态。

## 强制治理规则

- 任何任务必须明确：执行人、预检人、最终验收人、发给谁、验收包、下一步唯一动作。
- 没有验收包 = 任务未完成。
- 生成者不能最终放行自己的任务。
- 右侧 Codex / Claude 预检不是最终验收。
- ChatGPT / 用户确认前不得标绿或最终收口。
